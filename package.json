{
  "name": "sam",
  "workspaces": [
    "app",
    "functions"
  ],
  "scripts": {
    "setup": "npm install",
    "app": "npm -w app",
    "functions": "npm -w functions",
    "rules:deploy": "firebase deploy --only firestore:rules,storage"
  },
  "dependencies": {
    "firebase-tools": "^11.23.1"
  }
}

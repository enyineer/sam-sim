{
  "name": "sam",
  "scripts": {
    "setup": "pnpm install",
    "app": "pnpm -w app",
    "functions": "pnpm -w functions",
    "rules:deploy": "firebase deploy --only firestore:rules,storage"
  },
  "dependencies": {
    "firebase-tools": "^11.30.0"
  }
}
